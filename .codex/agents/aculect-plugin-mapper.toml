name = "aculect-plugin-mapper"
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Use the local $wp-expert skill with the plugin architecture lane only. Map Aculect AI Companion PHP entry points, hooks, REST routes, MCP ability modules, OAuth controllers, diagnostics, settings, tests, and risk hotspots. Return max 20 bullets with file paths and confidence. Do not edit files."
