# 0.8.0 audit remediation

Target: `release/0.8.0`, starting at `62747fc926dd950f43648286a4c43637ed314cea`.
Owner approved implementation and direct pushes; no release or deployment is requested.

## Ordered acceptance checklist

- [x] Preserve memory identity and concurrent writes during resumable migration.
- [x] Report learning-review persistence failures and permit safe retries.
- [x] Guarantee WebMCP output-budget termination and bounded DOM inspection.
- [x] Carry namespace and optimistic version through memory administration.
- [x] Validate search cursors and align pagination documentation.
- [x] Apply temporal validity consistently to retrieval and synchronization.
- [x] Expose blocked migrations with bounded retries and maintenance recovery.
- [x] Reuse memory collaborators and measure batch overhead.
- [x] Reduce admin query fields and repeated counts with safe invalidation.
- [x] Complete a bounded, opt-in site-owned memory synchronization path; do not claim access to proprietary personal-memory stores.
- [x] Extract frontend features/styles and review lazy-loaded bundle costs.
- [x] Consolidate authoritative CI, artifact builds, and database proofs without reducing essential coverage.
- [x] Target expensive jobs using tested change detection, cache dependencies, and bound execution.
- [x] Review release packaging identity and required-check enforcement separately from publishing.
- [x] Fresh review, regression/runtime proof, full checks, direct push, and hosted verification.

Intelligence remains navigation/guidance metadata. Authorization remains in OAuth scopes, WordPress capabilities, and explicit site policy.

## Evidence

Implemented all code-level checklist areas above. Full hosted validation passed on `1ca0632a24e91caefcf63aa2c7d2b733691e4ead`; subsequent evidence-only documentation does not change the validated implementation.

Final source review approved `90b001941b3acdd04bffcf232c2578f68e8a034b` after correcting cross-namespace import identity. Full local PHPUnit: 1,316 tests / 26,112 assertions. Direct-push modularity ratchet passed. The subsequent frontend-only CI routing correction has a separate read-only review and all 13 CI regression tests pass.

First hosted run [34043521018](https://github.com/mehul0810/aculect-ai-companion/actions/runs/34043521018) passed PHP, assets, MySQL/MariaDB, SQLite, all three WordPress contracts, both security scans and canonical packaging. The packaged environment failed before plugin tests because wp-env did not initialize under Node 24. The follow-up restores the baseline Node 20.19 wp-env compatibility boundary after Node 24 dependency installation, with a regression test. This retained legacy runtime needs a separately validated wp-env upgrade; it is not the production plugin runtime.

Local evidence: full PHPStan analysis passed; production WPCS, PHP syntax, modularity, Composer validation, actionlint, JavaScript/style lint and production build passed. JavaScript regression suite: 94 passing tests. The independent Chromium WebMCP probe passed bounded output, hidden/form content exclusion, URL sanitization and large-DOM traversal against asset SHA256 `88dfc573f72695bf6dfe8377254783b279baf5b8d9a348c7c0f1ea3992edf49f`.

The main Studio admin preview returned a TLS protocol error; no security bypass or main-site test mutations were attempted. Real MySQL/MariaDB/SQLite/WordPress and packaged browser tests remain hosted checks, not claimed local successes. The new memory database proof reuses the existing packaged WordPress environment.

Final hosted [run 34063927627](https://github.com/mehul0810/aculect-ai-companion/actions/runs/34063927627): all 14 jobs passed, including Required CI. The packaged WordPress log confirms the 100-record batch cap and constant engine lookups, failed learning-option rollback and one-event retry, production migration CAS with a second real database connection, sync consent/privacy/validity/replay, and the workflow-admin Chromium proof. Plugin Check passed against the same canonical ZIP. WP-CLI uses its documented [include mode](https://developer.wordpress.org/cli/commands/eval-file/) for the strict-types test file. No assertions or required jobs were removed to obtain a pass.

The final full run consumed 1,123 summed job-seconds (about 18.7 job-minutes), with about 5 minutes 52 seconds wall time from the first job start to the aggregate completion. This is one observed run, not a billing guarantee or controlled benchmark.

Remaining operational limits: sync is opt-in and caller-driven, with five-minute snapshot refresh and eventual consistency, not access to provider-owned personal memory. DataViews remains a large lazy-loaded dependency. Legacy frontend files are smaller but still need incremental extraction; reduced line ceilings prevent regrowth. Required-check repository enforcement needs separate owner approval. No release, deployment or repository-settings change is included.

Additional follow-up identified during review: the bounded learning queue still uses one WordPress option. Failure atomicity is repaired, but concurrent unrelated queue writers can still overwrite one another; row-level queue revisions are a separate architectural improvement, not a guarantee made by this patch.

The release lockfile dependency audit also reports five development-tool advisories (one high, three moderate, one low: fast-uri, body-parser, express, qs and postcss-selector-parser). They are separate from GitHub's 43-alert default-branch notice. Production-only npm audit reports none, but that is not a clean full dependency audit; dependency/toolchain upgrades remain follow-up work.
